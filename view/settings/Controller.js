jQuery.sap.declare("view.settings.Controller");

// Provides controller util.Controller
sap.ui.define(["jquery.sap.global", "com/ffa/dash/util/Controller"],
  function(jQuery, UtilController) {
  "use strict";

  var Controller = UtilController.extend("view.settings.Controller", /** @lends view.settings.Controller */ {

  });
});
