// app/routes/index.js
module.exports = function(app, passport) {

  // =====================================
  // API =================================
  // =====================================
  require('./api.js')(app);

  // =====================================
  // T&C =================================
  // =====================================
  require('./terms.js')(app);

  // =====================================
  // PAYMENTS ============================
  // =====================================
  require('./braintree.js')(app, passport);

  // =====================================
  // XSODATA Proxying ====================
  // =====================================
  require('./xsodata.js')(app, passport);

  // =====================================
  // AUTH ================================
  // =====================================
  require('./auth.js')(app, passport);

};
